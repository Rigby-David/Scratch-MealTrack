// import functions and grab DOM elements
import { renderIngredients, displayIngredients } from './utils.js';

const form = document.getElementById('ass-ingredient');
const ingredientList = document.getElementById('ingredient-list');

const remove = document.getElementById('remove-ingredient');
const save = document.getElementById('add-ingredient');
// let state
let ingredientArr = [];

// set event listeners 
function renderIngredient() {

}

function displayIngredient() {

}

form.addEventListener('submit', (e) => {
    e.preventDefault();
    
})
  // get user input
  // use user input to update state 
  // update DOM to reflect the new state
